<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_round"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="30dp">

    <TextView
        style="@style/Roboto_TextView_Light_dark"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:text="@string/please_enter_cancellation_reason"
        android:textAllCaps="true" />

    <EditText
        android:id="@+id/reason"
        style="@style/Roboto_TextView_Light_dark"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_marginTop="20dp"
        android:background="@color/gray"
        android:gravity="top|left"
        android:hint="Reason Enter Here"
        android:imeOptions="actionNext"
        android:inputType="textCapWords|textCapSentences|textMultiLine"
        android:maxLength="100"
        android:maxLines="5"
        android:padding="10dp" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/submit"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:backgroundTint="@color/colorPrimaryDark"
        android:onClick="saveDeviceInfo"
        android:text="Submit"
        android:textColor="@color/white" />

</LinearLayout>